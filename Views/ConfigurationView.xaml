<Window x:Class="TP2_DetectionLangue.Views.ConfigurationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TP2_DetectionLangue.Views"
        xmlns:viewmodels="clr-namespace:TP2_DetectionLangue.ViewModels"
        mc:Ignorable="d"
        Title="Configuration" Height="350" Width="360">

    <Window.DataContext>
         <viewmodels:ConfigurationViewModel/>
     </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <WrapPanel HorizontalAlignment="Center">
                <Label Content="*Langue" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <ComboBox HorizontalAlignment="Left" VerticalAlignment="Center" Width="80"/>
            </WrapPanel> 
        </StackPanel>

        <StackPanel Grid.Row="1">
            <WrapPanel HorizontalAlignment="Center">
                <Label Content="Jeton :" Margin="8,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <TextBox HorizontalAlignment="Left" VerticalAlignment="Center" Width="80"/>
            </WrapPanel> 
        </StackPanel>
        <StackPanel Grid.Row="2" VerticalAlignment="Bottom">
            <WrapPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <TextBlock TextWrapping="Wrap" VerticalAlignment="Center">
                     Les parammètres marqués d'une * néccessitent 
                     un redémarrage de l'application.       
                </TextBlock>
            </WrapPanel>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center">
            <WrapPanel HorizontalAlignment="Center">
                <Button Content="Sauver" Width="100" Margin="0,10,10,10"/>
                <Button Content="Annuler" Width="100" Margin="0,10,10,10"/>
            </WrapPanel>
        </StackPanel>
</Grid>

</Window>
