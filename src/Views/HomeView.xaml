<UserControl x:Class="LanguageDetector.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LanguageDetector.Views"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="920">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Label Content="Entrez le texte pour lequel vous voulez détecter la langue : " Margin="10,20,0,20"/>
            <TextBox Height="330" Margin="10,0,0,0" 
                     Text="{Binding TextToDetect, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     TextWrapping="Wrap"
            />

            <Rectangle Height="30"/>
            <Button Content="Détecter" Width="200" Padding="10,5" Command="{Binding DetectCommand}"/>
        </StackPanel>
       <StackPanel Grid.Column="1" Margin="10,0,0,0">
            <Label Content="Résultat de la détection :" Margin="0,20,0,20"/>

            <ComboBox ItemsSource="{Binding DetectedLanguages}" 
                      SelectedItem="{Binding SelectedLanguage}" 
                      DisplayMemberPath="FullLanguageName"
                      Width="200"
                      Margin="0,0,0,20"/>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <!-- largeur fixe pour que les labels soient alignés -->
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Langue:" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedLanguage.FullLanguageName}"/>

                <Label Grid.Row="1" Grid.Column="0" Content="Score (0-1):" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedLanguage.Score}"/>
            </Grid>
        </StackPanel>   
    </Grid>
</UserControl>
